export default class ExamRegistration{
        nic:number;
        examName:string;

    constructor(nic: number, examName: string) {
            this.nic=nic;
            this.examName=examName;
        }
}